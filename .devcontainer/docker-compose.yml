version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
      - cargo-cache:/home/dev/.cargo
      - rustup-cache:/home/dev/.rustup
    command: sleep infinity

volumes:
  cargo-cache:
  rustup-cache:
