{
    "name": "go-suspend-on-idle",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "remoteUser": "dev",
    "workspaceFolder": "/workspace",
    "postStartCommand": "/startup.sh",
    "waitFor": "postStartCommand",
    "capAdd": ["NET_ADMIN"],
    "customizations": {
        "vscode": {
            "extensions": [
                "rust-lang.rust-analyzer",
                "tamasfe.even-better-toml"
            ]
        }
    }
}
